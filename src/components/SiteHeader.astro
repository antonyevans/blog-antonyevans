<header class="site-header">
  <div class="container inner">
    <a class="brand" href="/">Antony Evans</a>
    <nav>
      <a href="/blog/">Writing</a>
      <a href="/projects/">Projects</a>
      <a href="/search/">Search</a>
      <a href="/about/">About</a>
      <a href="/rss.xml">RSS</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 10;
    backdrop-filter: blur(12px);
    background: color-mix(in oklab, var(--primary) 92%, black 8%);
    border-bottom: 1px solid color-mix(in oklab, var(--accent) 34%, var(--primary) 66%);
  }

  .inner {
    min-height: 4rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .brand {
    font-family: 'Space Grotesk', system-ui, sans-serif;
    font-size: 1.18rem;
    color: var(--brand-white, #ffffff);
    font-weight: 700;
    text-decoration: none;
  }

  nav { display: flex; gap: 1rem; }
  nav a {
    color: color-mix(in oklab, var(--bg) 88%, white 12%);
    text-decoration: none;
    font-weight: 700;
    font-size: 0.92rem;
  }
  nav a:hover { color: var(--accent); }

  @media (max-width: 720px) {
    .inner { flex-direction: column; padding: 0.8rem 0; }
    nav {
      width: 100%;
      justify-content: center;
      flex-wrap: wrap;
      gap: 0.45rem 0.9rem;
    }
  }
</style>
